import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Premium IPTV Services - The #1 Provider in Europe",
  description: "Learn about our mission to redefine television through high-availability infrastructure and premium content delivery.",
};

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return children;
}
