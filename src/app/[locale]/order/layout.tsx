import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Premium IPTV Checkout - Secure Your Subscription",
  description: "Finalize your premium IPTV subscription. Fast delivery, secure connection, and 24/7 technical support included with every plan.",
};

export default function OrderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
