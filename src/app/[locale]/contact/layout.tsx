import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Support - Premium IPTV Services",
  description: "Get in touch with our 24/7 technical support team for assistance with setup, billing, or channel requests.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
